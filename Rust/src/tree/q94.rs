use crate::data_structure::data_structure::TreeNode;
use std::cell::RefCell;
use std::rc::Rc;

pub struct Solution1 {}

impl Solution1 {
    pub fn inorder_traversal(root: Option<Rc<RefCell<TreeNode>>>) -> Vec<i32> {
        vec![]
    }
}
